$(document).ready(function () {
  /* $.ajax({
        type: "POST",
        url: "../../../api/costUserAccess",
        success: function(r) {
            if (r[0].create_product == 0)
                $('.costCreateProducts').remove();
            if (r[0].create_materials == 0)
                $('.costCreateMaterials').remove();
            if (r[0].create_machines == 0)
                $('.costCreateMachines').remove();
            if (r[0].create_process == 0)
                $('.costCreateProcess').remove();
    
            if (r[0].product_materials == 0)
                $('.productsMaterials').remove();
            if (r[0].product_process == 0)
                $('.productsProcess').remove();
            if (r[0].create_process == 0)
                $('.servicesExternal').remove();
            if (r[0].create_process == 0)
                $('.linesProducts').remove();
    
            if (r[0].general_data == 0)
                $('.generalData').remove();
            if (r[0].payroll == 0)
                $('.payroll').remove();
            if (r[0].general_expenses == 0)
                $('.generalExpenses').remove();
            if (r[0].distribution_expenses == 0)
                $('.distributionExpenses').remove();
            if (r[0].users == 0)
                $('.users').remove();
        } 
    });*/
});
